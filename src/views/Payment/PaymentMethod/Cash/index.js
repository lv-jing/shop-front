import React, { useState } from 'react';
import { FormattedMessage } from 'react-intl-phraseapp';
import { handleEmailShow } from '@/utils/utils';

const Cash = ({ billingJSX }) => {
  return <>{billingJSX}</>;
};

export default Cash;
