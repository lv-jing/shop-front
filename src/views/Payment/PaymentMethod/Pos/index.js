import React, { useState } from 'react';
import { FormattedMessage } from 'react-intl-phraseapp';
import { handleEmailShow } from '@/utils/utils';

const Pos = ({ billingJSX }) => {
  return <>{billingJSX}</>;
};

export default Pos;
